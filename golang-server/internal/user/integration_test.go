package user

import "testing"

func TestCreateUser(t *testing.T) {
	t.Run("Create User with invalid name", func(t *testing.T) {
	})

}

func NewUserServer() {
	// store in test container
	// domain -> real domain
}
